# MNU Events - Backend Environment Variables
# 
# ИНСТРУКЦИЯ:
# 1. Скопируйте этот файл: cp .env.example .env
# 2. Заполните все значения ниже
# 3. НИКОГДА не коммитьте .env файл в Git!
#
# Подробная инструкция: backend/ENV_SETUP.md

# БАЗА ДАННЫХ (обязательно)
DATABASE_URL=postgresql://mnu_user:mnu_password@localhost:5432/mnu_events_dev

# ПРИЛОЖЕНИЕ
PORT=3001
NODE_ENV=development

# JWT АУТЕНТИФИКАЦИЯ (обязательно - сгенерируйте случайную строку 32+ символа!)
JWT_SECRET=RzQ5OGYxMDI5MGNjMGIxYzdkZDI0NGEwNDVmNmQwN2RhMmQzMQ==
JWT_EXPIRATION=1h

# REFRESH TOKEN (обязательно - ДРУГОЙ ключ!)
REFRESH_TOKEN_SECRET=ZTAwMWY2NDdhMjA3MzY5YjE4YjRlNjQ2NmEyZjgwMDM3M2I0NjEwMTk1YjY3MA==
REFRESH_TOKEN_EXPIRATION=7d

# EMAIL VERIFICATION (обязательно - ЕЩЕ ОДИН ДРУГОЙ ключ!)
EMAIL_VERIFICATION_SECRET=YTRlMzU2Y2M0ZTJmNTkwZjZkZjgyNTJmODU1Y2JkMzUyNjU5N2YxYzQzYzU0OTcwYg==
EMAIL_VERIFICATION_EXPIRATION=24h

# PAYMENT QR CODE SIGNING (REQUIRED - Generate with: openssl rand -hex 32)
# КРИТИЧЕСКИ ВАЖНО: используется для подписи QR-кодов билетов
PAYMENT_SECRET=<REQUIRED: Generate with: openssl rand -hex 32>

# CSRF PROTECTION (REQUIRED - Generate with: openssl rand -hex 32)
# КРИТИЧЕСКИ ВАЖНО: требуется для запуска приложения
CSRF_SECRET=<REQUIRED: Generate with: openssl rand -hex 32>

# SMTP (для отправки email - опционально)
# Для Gmail используйте App Password: https://support.google.com/accounts/answer/185833
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password-here
EMAIL_FROM=your-email@gmail.com

# CORS
CORS_ORIGIN=http://localhost:5173

# RATE LIMITING
THROTTLE_TTL=60
THROTTLE_LIMIT=100

# ============================================
# REDIS (Required for caching and sessions)
# ============================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ============================================
# FILE UPLOADS
# ============================================
MAX_FILE_SIZE=5242880
UPLOAD_DIR=./uploads

# ============================================
# LOGGING
# ============================================
LOG_LEVEL=info
LOG_FILE_ENABLED=false
LOG_FILE_PATH=logs

# ============================================
# FRONTEND URL (for email links, payment redirects)
# ============================================
FRONTEND_URL=http://localhost:5173

# ============================================
# PRODUCTION SECURITY CHECKLIST
# ============================================
# Before deploying to production, ensure:
# ✓ All secrets regenerated (never use example values!)
# ✓ NODE_ENV=production
# ✓ SMTP credentials configured
# ✓ CORS_ORIGIN set to production domain
# ✓ DATABASE_URL points to production database
# ✓ FRONTEND_URL is production URL
# ✓ LOG_FILE_ENABLED=true
# ✓ REDIS connection configured
# ✓ All secrets are 32+ characters and cryptographically random