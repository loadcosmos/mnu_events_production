# План Быстрого Деплоя MNU Events (Demo для Университета)

**Проект:** MNU Events Platform (Capstone университетский проект)
**Статус:** Готов к быстрому деплою
**Дата:** 2025-12-01

---

## РЕАЛЬНАЯ ЗАДАЧА

🎯 **Что нужно:** Задеплоить → Показать 20-30 студентам → Получить фидбек от университета
⏱️ **Время:** 30-40 минут на деплой + 1-2 часа на подготовку
💰 **Стоимость:** $0 (бесплатные тарифы)
👥 **Масштаб:** 20-30 студентов (не тысячи пользователей!)

**ВСЁ ОСТАЛЬНОЕ (CI/CD, security, docs) - ПОСЛЕ ОДОБРЕНИЯ УНИВЕРСИТЕТА**

---

## Выбранное Решение

### 🏆 Vercel (Frontend) + Railway (Backend + DB)

**Почему именно это:**
- ⚡ **Самый быстрый** способ задеплоить (30-40 минут)
- 💰 **Бесплатно** для 20-30 студентов
- 🎓 **Не нужны DevOps навыки** - все через веб-интерфейс
- 🔄 **Легко мигрировать** на другие платформы потом

---

## Простой План (3 Шага)

### ШАГ 1: Подготовка (1-2 часа)

**1.1 Проверить что проект работает локально**

```bash
cd /home/loadcosmos/mnu_events

# Запустить проект
./start.sh

# Проверить:
# - Frontend: http://localhost:5173 открывается ✓
# - Backend API: http://localhost:3001/api/docs (Swagger) работает ✓
# - Login/Register работает ✓
# - Создание события работает ✓
```

**1.2 Убедиться что .env.example актуален**

```bash
# Проверить backend/.env.example
cat backend/.env.example

# Должны быть все переменные:
# - DATABASE_URL
# - JWT_SECRET
# - REFRESH_TOKEN_SECRET
# - EMAIL_VERIFICATION_SECRET
# - SMTP_* (если используешь email)
# - CORS_ORIGIN
```

**1.3 Создать аккаунты (5 минут)**

- Railway: https://railway.app (Sign up with GitHub)
- Vercel: https://vercel.com (Sign up with GitHub)

**Готово!** Переходим к деплою.

---

### ШАГ 2: Deploy Backend на Railway (15-20 минут)

**2.1 Создать проект**

1. Зайти на https://railway.app
2. New Project → Deploy from GitHub repo
3. Выбрать ваш репозиторий `mnu_events`
4. Root Directory: `backend`

**2.2 Добавить PostgreSQL**

1. В Railway проекте нажать "+" (New)
2. Database → PostgreSQL
3. Railway автоматически создаст переменную `DATABASE_URL`

**2.3 Добавить Redis (опционально)**

1. Нажать "+" → Database → Redis
2. Railway автоматически создаст переменную `REDIS_URL`

**2.4 Настроить Environment Variables**

В Railway → Settings → Variables:

```bash
# Обязательные (генерировать случайно!)
JWT_SECRET=ваш_случайный_секрет_64_символа
REFRESH_TOKEN_SECRET=другой_случайный_секрет_64_символа
EMAIL_VERIFICATION_SECRET=еще_один_случайный_секрет_64_символа

# CORS (добавим URL Vercel после деплоя frontend)
CORS_ORIGIN=*

# Email (если используете)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Другие
NODE_ENV=production
PORT=3001
```

**Сгенерировать секреты:**
```bash
# На локальной машине
openssl rand -base64 48  # Для JWT_SECRET
openssl rand -base64 48  # Для REFRESH_TOKEN_SECRET
openssl rand -base64 48  # Для EMAIL_VERIFICATION_SECRET
```

**2.5 Deploy!**

1. Railway автоматически задеплоит
2. Подождать 2-3 минуты
3. Скопировать URL (например: `mnu-events-backend.up.railway.app`)

**2.6 Запустить Migrations и Seed**

В Railway Dashboard:

1. Зайти в ваш backend service
2. Settings → Deploy Logs (проверить что migrations запустились)
3. Если нет - открыть Console и запустить:
   ```bash
   npx prisma migrate deploy
   npx prisma db seed
   ```

**Backend готов!** ✅

---

### ШАГ 3: Deploy Frontend на Vercel (10-15 минут)

**3.1 Import проекта**

1. Зайти на https://vercel.com
2. Add New → Project
3. Import Git Repository → выбрать `mnu_events`
4. Configure Project:
   - Framework Preset: **Vite**
   - Root Directory: **frontend**
   - Build Command: `npm run build` (auto-detected)
   - Output Directory: `dist` (auto-detected)

**3.2 Настроить Environment Variable**

В Vercel во время импорта или потом в Settings → Environment Variables:

```bash
VITE_API_URL=https://mnu-events-backend.up.railway.app
```

(Замените на ваш URL из Railway)

**3.3 Deploy!**

1. Нажать "Deploy"
2. Подождать 1-2 минуты
3. Vercel даст URL (например: `mnu-events.vercel.app`)

**3.4 Обновить CORS на Backend**

1. Вернуться в Railway → Backend service → Variables
2. Обновить `CORS_ORIGIN`:
   ```bash
   CORS_ORIGIN=https://mnu-events.vercel.app
   ```
3. Railway автоматически передеплоит

**Frontend готов!** ✅

---

### ШАГ 4: Тестирование (10-15 минут)

**4.1 Smoke Testing**

Открыть `https://mnu-events.vercel.app` и проверить:

- [ ] Frontend загружается
- [ ] Регистрация работает (создать тестового студента)
- [ ] Login работает
- [ ] Создать событие (если есть права)
- [ ] Просмотр событий работает
- [ ] Регистрация на событие работает
- [ ] QR код генерируется

**4.2 Создать тестовые данные**

1. Создать 3-5 событий
2. Зарегистрироваться на них как студент
3. Проверить что всё отображается корректно

**4.3 Дать доступ 20-30 студентам**

1. Поделиться ссылкой: `https://mnu-events.vercel.app`
2. Дать тестовые аккаунты или попросить зарегистрироваться
3. Собрать фидбек!

**ГОТОВО!** 🎉

---

## Возможные Проблемы и Решения

### Backend не запускается на Railway

**Проблема:** Ошибка при деплое

**Решение:**
```bash
# Проверить логи в Railway Dashboard → Deployments → View logs
# Частые причины:
# 1. Отсутствует DATABASE_URL → добавить PostgreSQL service
# 2. Migrations не запустились → запустить вручную через Console
# 3. Port не тот → убедиться что в коде используется process.env.PORT
```

### Frontend не может подключиться к Backend

**Проблема:** CORS errors в browser console

**Решение:**
```bash
# 1. Проверить CORS_ORIGIN в Railway включает Vercel URL
# 2. Проверить VITE_API_URL в Vercel правильный (без trailing slash)
# 3. Проверить что backend действительно работает (открыть /api/docs)
```

### Database миграции не применились

**Проблема:** Ошибка "Table does not exist"

**Решение:**
```bash
# В Railway Console:
npx prisma migrate deploy

# Потом seed:
npx prisma db seed
```

### Email не отправляется

**Проблема:** Email verification не работает

**Решение (временно для демо):**
```typescript
// backend/src/users/users.service.ts
// Закомментировать проверку isEmailVerified для демо
// Или вручную подтвердить email в БД через Railway DB console
```

---

## После Одобрения Университета

Когда университет одобрит проект и вы получите фидбек, вот что нужно сделать:

### Фаза 2: Production-готовность (1-2 недели)

**1. Security Fixes**
- Webhook verification (KASPI payments)
- IDOR protection (resource ownership)
- Race conditions (event registrations)

**2. Отдельный Production Репозиторий**
- Создать `mnu-events-production`
- Без истории разработки
- Clean git history

**3. CI/CD Pipeline**
- GitHub Actions для автоматического тестирования
- Automated deployment на push

**4. Документация**
- DEPLOYMENT.md - пошаговый гайд
- TEAM_SETUP.md - для новых разработчиков
- UNIVERSITY_HANDOVER.md - для передачи университету

**5. Backup & Monitoring**
- Automated database backups
- Error tracking (Sentry)
- Uptime monitoring

**6. Передача Университету**
- GitHub organization transfer
- Railway/Vercel team invites
- Training session для IT отдела

**Но это всё - ПОТОМ!** Сейчас фокус на demo.

---

## Временная Шкала

```
┌─────────────────────────────────────────────────────────┐
│ СЕГОДНЯ (1-2 часа):                                     │
│ ✓ Подготовка - проверить что всё работает локально     │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ ЗАВТРА (30-40 минут):                                   │
│ ✓ Deploy на Railway (backend + DB)                     │
│ ✓ Deploy на Vercel (frontend)                          │
│ ✓ Smoke testing                                         │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ ПОСЛЕЗАВТРА:                                            │
│ ✓ Дать доступ 20-30 студентам                          │
│ ✓ Собрать фидбек                                        │
│ ✓ Презентовать университету                            │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ ПОСЛЕ ОДОБРЕНИЯ (1-2 недели):                          │
│ ✓ Security, CI/CD, docs, production repo               │
└─────────────────────────────────────────────────────────┘
```

**Итого:** 2-3 дня от "ничего" до "работающий demo с фидбеком"

---

## Стоимость

**Railway Free Tier:**
- $5 credit (хватит на 1-2 месяца для демо)
- Shared resources
- 512MB RAM
- 1GB PostgreSQL storage

**Vercel Free Tier:**
- Unlimited deployments
- 100GB bandwidth/month
- Automatic SSL

**Когда вырастет (после одобрения):**
- Railway: ~$5-10/месяц
- Vercel: бесплатно (или $20/месяц для team features)

---

## Критические Файлы (Минимум для Деплоя)

Эти файлы УЖЕ ЕСТЬ в проекте - ничего не нужно создавать:

✅ **backend/package.json** - Railway использует для build
✅ **backend/prisma/schema.prisma** - для migrations
✅ **backend/prisma/seed.ts** - для тестовых данных
✅ **frontend/package.json** - Vercel использует для build
✅ **frontend/vite.config.js** - конфиг Vite
✅ **.gitignore** - секреты защищены

**Что может понадобиться:**

📝 **backend/.env.example** - убедиться что актуален (уже есть)
📝 **frontend/.env.example** - пример для VITE_API_URL (создать если нет)

---

## Следующий Шаг

**Прямо сейчас:**

1. Проверить что проект работает локально:
   ```bash
   cd /home/loadcosmos/mnu_events
   ./start.sh
   ```

2. Создать аккаунты:
   - Railway: https://railway.app
   - Vercel: https://vercel.com

3. Сказать "готов к деплою" и я помогу с каждым шагом!

---

**Конец Простого Плана**

*Этот план создан для быстрого demo деплоя MNU Events. Фокус на скорости и простоте, а не на enterprise features. Всё остальное - после одобрения университета.*

**Готовы начать? Скажите "да" и начнём с проверки локального проекта!**
