# ============================================
# MNU Events - Environment Variables
# ============================================
#
# ИНСТРУКЦИЯ:
# 1. Скопируйте этот файл: cp .env.example .env
# 2. Заполните значения ниже согласно вашему окружению
# 3. НИКОГДА не коммитьте .env файл в Git!

# ============================================
# DEVELOPMENT MODE (Localhost)
# ============================================
# Для разработки на локальной машине

# Backend URL для Vite proxy (только dev mode)
# Vite проксирует /api -> VITE_BACKEND_URL/api
VITE_BACKEND_URL=http://localhost:3001

# Frontend API URL (не используется в dev mode благодаря proxy)
# Оставьте пустым - будет использоваться относительный путь /api
# VITE_API_URL=

# ============================================
# DEVELOPMENT MODE (Network / Mobile Testing)
# ============================================
# Для тестирования с телефона через локальную сеть

# 1. Узнайте ваш локальный IP:
#    - Linux/macOS: ip addr show | grep "inet 192"
#    - Windows: ipconfig | findstr "IPv4"
# 2. Замените 192.168.1.67 на ваш IP

# Backend URL для Vite proxy
# VITE_BACKEND_URL=http://192.168.1.67:3001

# Также обновите backend/.env:
# CORS_ORIGIN=https://192.168.1.67:5173,http://192.168.1.67:5173,https://localhost:5173,http://localhost:5173

# ============================================
# PRODUCTION MODE
# ============================================
# Для production деплоя (Vercel, Netlify, Railway и т.д.)

# Frontend API URL (ОБЯЗАТЕЛЬНО для production!)
# Полный URL к вашему backend API (С /api в конце!)
# VITE_API_URL=https://api.mnu-events.com/api

# VITE_BACKEND_URL не используется в production (только для dev proxy)

# ============================================
# Примеры для разных платформ
# ============================================

# Vercel (Frontend) + Railway (Backend):
# VITE_API_URL=https://mnu-events-api.up.railway.app/api

# Netlify (Frontend) + Render (Backend):
# VITE_API_URL=https://mnu-events-api.onrender.com/api

# Same-origin deployment (Frontend и Backend на одном домене):
# VITE_API_URL=/api

# ============================================
# ВАЖНО для Production
# ============================================
# 1. Убедитесь что backend в production mode (NODE_ENV=production)
# 2. Backend должен быть доступен по HTTPS
# 3. Настройте CORS_ORIGIN в backend/.env для вашего frontend домена
# 4. Cookies будут автоматически secure=true в production
